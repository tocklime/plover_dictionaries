git repo for my own plover dictionaries, as well as pointers to the ones I depend on. 

After cloning:

* `git submodule init`
* `git submodule update`

To update submodules

* `git submodule update --remote`
